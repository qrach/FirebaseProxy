{
  "name": "firebaseproxy",
  "version": "1.0.0",
  "description": "A proxy using the free hosting of Google Firebase and the free workflow of Github actions.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i --package-lock && npm ci"
  },
  "author": "Qrach",
  "license": "ISC"
}
