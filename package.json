{
  "name": "firebaseproxy",
  "version": "1.0.0",
  "description": "A proxy using the free hosting of Google Firebase and the free workflow of Github actions.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "egrep -R --exclude-dir=node_modules '=\s*require\s*\(' | awk '{gsub(/^.+require\s*\(\s*./,""); gsub(/.\s*\).*$/,""); print $0}' | xargs npm install && npm in --package-lock && npm ci"
  },
  "author": "Qrach",
  "license": "ISC"
}
