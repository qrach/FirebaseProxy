{
  "name": "firebaseproxy",
  "version": "1.0.0",
  "description": "A proxy using the free hosting of Google Firebase and the free workflow of Github actions.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && npm ci",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.2",
    "http-proxy": "^1.18.1"
  },
  "author": "Qrach",
  "license": "ISC"
}
